name: Deploy Laravel

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: 🚀 Conectar via SSH e fazer deploy
        uses: appleboy/ssh-action@v0.1.10
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_KEY }}
          port: 22
          script: |
            set -e

            echo "➡️ Entrando na pasta do projeto"
            cd /var/www/meu_projeto

            echo "➡️ Atualizando branch main"
            git fetch origin main
            git reset --hard origin/main
            git clean -fd

            echo "➡️ Instalando dependências"
            composer install --no-interaction --prefer-dist --optimize-autoloader

            echo "➡️ Rodando migrações"
            php artisan migrate --force

            echo "➡️ Limpando e otimizando caches"
            php artisan config:clear
            php artisan config:cache
            php artisan route:cache
            php artisan view:clear
            php artisan view:cache

            echo "➡️ Reiniciando serviço do PHP (ajuste se for outra versão)"
            sudo systemctl restart php8.2-fpm

            echo "✅ Deploy concluído com sucesso!"
